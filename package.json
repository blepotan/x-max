{
  "name": "x-max-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free Manifest V3 X scheduling shortcut extension",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check src/settings.js && node --check src/scheduling.js && node --check src/selectors.js && node --check src/content-script.js && node --check src/service-worker.js && node --check src/options.js"
  }
}
